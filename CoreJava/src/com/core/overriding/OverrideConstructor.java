package com.core.overriding;

/**
 * Constructors cannot be overridden. Because constructors are not methods and a
 * subclass’ constructor cannot have same name as a superclass’ one, so there’s
 * nothing relates between constructors and overriding.
 *
 */
public class OverrideConstructor {

}
